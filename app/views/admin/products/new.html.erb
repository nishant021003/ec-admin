<h1 class="mb-4"><%= t('page_titles.new_product') %></h1>

<div class="card">
  <div class="card-body">
    <%= render "form", product: @product %>
  </div>
</div>

<%= link_to "← #{t('products.back_to_products')}", admin_products_path, class: "btn btn-secondary mt-3" %>
