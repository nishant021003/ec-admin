<h1 class="mb-4"><%= t('page_titles.edit_product') %></h1>

<div class="card">
  <div class="card-body">
    <%= render "form", product: @product %>
  </div>
</div>

<%= link_to "← #{t('page_titles.back_to_product')}", admin_product_path(@product), class: "btn btn-secondary mt-3" %>
