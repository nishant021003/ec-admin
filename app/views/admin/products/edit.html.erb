<h1 class="mb-4">Edit Product</h1>

<div class="card">
  <div class="card-body">
    <%= render "form", product: @product %>
  </div>
</div>

<%= link_to "← Back to product", admin_product_path(@product), class: "btn btn-secondary mt-3" %>
