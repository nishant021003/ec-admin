<h1 class="mb-4"><%= t('page_titles.edit_customer') %></h1>

<div class="card">
  <div class="card-body">
    <%= render "form", customer: @customer %>
  </div>
</div>

<%= link_to "← #{t('page_titles.back_to_customer')}", admin_customer_path(@customer), class: "btn btn-secondary mt-3" %>
