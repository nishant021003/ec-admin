<h1 class="mb-4">Edit Customer</h1>

<div class="card">
  <div class="card-body">
    <%= render "form", customer: @customer %>
  </div>
</div>

<%= link_to "← Back to customer", admin_customer_path(@customer), class: "btn btn-secondary mt-3" %>
