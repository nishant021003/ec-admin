<h1 class="mb-4"><%= t('page_titles.new_customer') %></h1>

<div class="card">
  <div class="card-body">
    <%= render "form", customer: @customer %>
  </div>
</div>

<%= link_to "← #{t('customers.back_to_customers')}", admin_customers_path, class: "btn btn-secondary mt-3" %>
