<h1 class="mb-4"><%= t('page_titles.new_coupon') %></h1>

<div class="card">
  <div class="card-body">
    <%= render "form", coupon: @coupon %>
  </div>
</div>

<%= link_to "← #{t('coupons.back_to_coupons')}", admin_coupons_path, class: "btn btn-secondary mt-3" %>
