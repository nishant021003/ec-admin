<h1 class="mb-4"><%= t('page_titles.edit_coupon') %></h1>

<div class="card">
  <div class="card-body">
    <%= render "form", coupon: @coupon %>
  </div>
</div>

<%= link_to "← #{t('page_titles.back_to_coupon')}", admin_coupon_path(@coupon), class: "btn btn-secondary mt-3" %>
