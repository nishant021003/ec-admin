<h1 class="mb-4"><%= t('page_titles.edit_category') %></h1>

<div class="card">
  <div class="card-body">
    <%= render "form", category: @category %>
  </div>
</div>

<%= link_to "← #{t('page_titles.back_to_category')}", admin_category_path(@category), class: "btn btn-secondary mt-3" %>
