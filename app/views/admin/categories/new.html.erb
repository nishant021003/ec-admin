<h1 class="mb-4"><%= t('page_titles.new_category') %></h1>

<div class="card">
  <div class="card-body">
    <%= render "form", category: @category %>
  </div>
</div>

<%= link_to "← #{t('categories.back_to_categories')}", admin_categories_path, class: "btn btn-secondary mt-3" %>
