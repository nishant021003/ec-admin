<h1 class="mb-4">New admin user</h1>

<div class="card">
  <div class="card-body">
    <%= render "form", user: @user %>
  </div>
</div>

<%= link_to "← Back to admin users", admin_users_path, class: "btn btn-secondary mt-3" %>
