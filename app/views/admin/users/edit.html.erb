<h1 class="mb-4"><%= t('page_titles.edit_user') %></h1>

<div class="card">
  <div class="card-body">
    <%= render "form", user: @user %>
  </div>
</div>

<%= link_to "← #{t('page_titles.back_to_users')}", admin_users_path, class: "btn btn-secondary mt-3" %>
