<h1 class="mb-4"><%= t('page_titles.edit_gift_card') %></h1>

<div class="card">
  <div class="card-body">
    <%= render "form", gift_card: @gift_card %>
  </div>
</div>

<%= link_to "← #{t('page_titles.back_to_gift_card')}", admin_gift_card_path(@gift_card), class: "btn btn-secondary mt-3" %>
