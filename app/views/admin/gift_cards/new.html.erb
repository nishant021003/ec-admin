<h1 class="mb-4"><%= t('page_titles.new_gift_card') %></h1>

<div class="card">
  <div class="card-body">
    <%= render "form", gift_card: @gift_card %>
  </div>
</div>

<%= link_to "← #{t('gift_cards.back_to_gift_cards')}", admin_gift_cards_path, class: "btn btn-secondary mt-3" %>
